module ImportsHelper
# deprecated - this is the same as gears_by_category(category_id), now in the gears_helper
#  def gear_by_category(category_id)
#    Gear.where("category_id" => category_id, "user_id" => current_user.id).order("upper(name)")
#  end

end
